<Window x:Class="CipherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CipherApp"
        xmlns:vm="clr-namespace:CipherApp.ViewModels"
        mc:Ignorable="d"
        Title="Şifreleme Atölyesi" Height="600" Width="1000" Background="#0F172A">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="#111827" CornerRadius="12" Padding="12" >
            <StackPanel>
                <TextBlock Text="Şifreler" Foreground="#E5E7EB" FontSize="18" FontWeight="Bold"/>
                <ListBox x:Name="CipherListBox" ItemsSource="{Binding CipherList}" SelectedItem="{Binding SelectedCipher}" Margin="0,8,0,0"
                         Background="#0B1220" BorderBrush="#0B1220" Foreground="#E5E7EB">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" Padding="8"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Separator Margin="0,12"/>
                <Button Content="Yeni Soru" Command="{Binding NewQuizCommand}" Background="#22C55E" Foreground="White" Padding="8"/>
            </StackPanel>
        </Border>

        <!-- Spacer -->
        <Grid Grid.Column="1"/>

        <!-- Content -->
        <Border x:Name="ContentPanel" Grid.Column="2" Background="#111827" CornerRadius="12" Padding="16" Opacity="0.0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding SelectedCipher.Name}" FontSize="22" FontWeight="Bold" Foreground="#F9FAFB"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: Inputs -->
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Açıklama" Foreground="#9CA3AF"/>
                        <TextBlock TextWrapping="Wrap" Foreground="#D1D5DB" Margin="0,4,0,12" Text="{Binding Explanation}"/>

                        <TextBlock Text="Anahtar" Foreground="#9CA3AF"/>
                        <TextBox Text="{Binding KeyInput, UpdateSourceTrigger=PropertyChanged}" Background="#0B1220" Foreground="#F9FAFB" BorderBrush="#374151"/>
                        <TextBlock Foreground="#6B7280" FontSize="12" Margin="0,2,0,12" Text="Sezar: sayı (3), Vigenère: kelime (ANAHTAR), Hill: '3 3 2 5'"/>

                        <TextBlock Text="Girdi" Foreground="#9CA3AF"/>
                        <TextBox Text="{Binding Plaintext, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="120" Background="#0B1220" Foreground="#F9FAFB" BorderBrush="#374151"/>

                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0" >
                            <Button Content="Şifrele" Command="{Binding EncryptCommand}" Background="#3B82F6" Foreground="White" Padding="8" Margin="0,0,8,0"/>
                            <Button Content="Çöz" Command="{Binding DecryptCommand}" Background="#8B5CF6" Foreground="White" Padding="8"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Right: Output & Quiz -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Çıktı" Foreground="#9CA3AF"/>
                        <TextBox Text="{Binding Output}" AcceptsReturn="True" Height="120" Background="#0B1220" Foreground="#F9FAFB" BorderBrush="#374151"/>

                        <Border Background="#0B1220" CornerRadius="8" Padding="12" Margin="0,12,0,0">
                            <StackPanel>
                                <TextBlock Text="Soru Üretici" Foreground="#E5E7EB" FontWeight="Bold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <TextBlock Text="Şifre:" Foreground="#9CA3AF"/>
                                    <TextBlock Text="{Binding CurrentQuestion.CipherName}" Foreground="#D1D5DB" Margin="6,0,0,0"/>
                                </StackPanel>
                                <TextBlock Text="Şifreli Metin:" Foreground="#9CA3AF"/>
                                <TextBlock Text="{Binding CurrentQuestion.Ciphertext}" TextWrapping="Wrap" Foreground="#FDE68A"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Content="Yeni Soru" Command="{Binding NewQuizCommand}" Background="#22C55E" Foreground="White" Padding="8"/>
                                    <Button Content="Cevabı Göster" Command="{Binding ShowAnswerCommand}" Background="#EF4444" Foreground="White" Padding="8" Margin="8,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
