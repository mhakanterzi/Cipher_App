<UserControl x:Class="CipherApp.Views.ReactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource Brush.Card}" CornerRadius="8" Padding="8" Margin="0,0,0,8">
            <TextBlock Text="React içerik köprüsü: Uygulama klasörüne 'wwwroot/index.html' koyarsan burada açılır."
                       Foreground="{StaticResource Brush.Text}"/>
        </Border>

        <Grid Grid.Row="1">
            <wv2:WebView2 x:Name="Web"/>
            <Border x:Name="EmptyState" Background="{StaticResource Brush.Card}" CornerRadius="8" Padding="16" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Henüz wwwroot/index.html bulunamadı." Foreground="{StaticResource Brush.Text}"/>
                    <TextBlock Text="React projenizi production build (dist) alıp wwwroot altına kopyalayın." Foreground="{StaticResource Brush.Muted}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

